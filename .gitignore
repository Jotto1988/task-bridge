node_modules/
*.log
.DS_Store

# local-project is its own isolated git repo, created and reset by
# runAgent.ts. Its .git directory should never be tracked by the outer
# task-bridge repo (that would create a nested "embedded repository").
local-project/.git/
